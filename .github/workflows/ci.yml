# SPDX-License-Identifier: GPL-3.0-or-later
name: Verify

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: verify-${{ github.repository }}-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  linux:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Check out source and Common
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Verify source identity
        shell: bash
        run: |
          set -euo pipefail
          version="$(tr -d '[:space:]' < VERSION)"
          [[ "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]
          grep -Fq "**Current source version:** $version" README.md
          test "$(tr -d '[:space:]' < src/infiltratr-common/VERSION)" = "1.19.24"
          test "$(git -C src/infiltratr-common rev-parse HEAD)" = "748e089ae175329471d4cf375522c44081371bd5"

      - name: Install build dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends appstream build-essential cmake debhelper dpkg-dev dbus-daemon gdb git gpgv libappstream-dev libcurl4-openssl-dev libgtk-3-dev libgtk-4-dev libglib2.0-dev libjson-glib-dev liblzma-dev libsqlite3-dev libxapp-dev pkg-config xvfb zlib1g-dev

      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --parallel 2

      - name: Test
        run: ctest --test-dir build --output-on-failure

      - name: Verify update authorization boundary
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import xml.etree.ElementTree as ET

          app = Path("src/app/main.cpp").read_text()
          theme = Path("src/app/theme.cpp").read_text()
          backend = Path("src/backends/apt/apt_backend.cpp").read_text()
          transaction = Path("src/engine/debian_transaction.cpp").read_text()
          candidate = Path("src/engine/debian_candidate.cpp").read_text()
          package_policy = Path("src/engine/package_policy.cpp").read_text()
          phased = Path("src/engine/debian_phased_updates.cpp").read_text()
          preferences = Path("src/engine/debian_preferences.cpp").read_text()
          reconcile = Path("src/engine/debian_reconcile.cpp").read_text()
          state_store = Path("src/engine/package_state_store.cpp").read_text()
          helper = Path("src/helper/update_helper.cpp").read_text()
          source_helper = Path("src/helper/source_helper.cpp").read_text()
          source_mutation = Path("src/sources/source_mutation.cpp").read_text()
          guard = Path("src/helper/apt_plan_guard.cpp").read_text()
          engine_client = Path("src/client/engine_client.cpp").read_text()
          engine_service = Path("src/service/main.cpp").read_text()
          engine_interface = Path("data/net.ssmith.infiltrator.software.Engine.xml").read_text()
          update_policy_path = Path("data/net.ssmith.infiltrator.software.updates.policy")
          source_policy_path = Path("data/net.ssmith.infiltrator.software.policy")
          policy = update_policy_path.read_text()

          assert "refresh_updates(state, true)" in app
          assert "engine.refresh(result->error)" in app
          assert "backend.refresh_metadata(result->error)" not in app
          assert '"refresh"}' not in app
          assert "AptBackend::refresh_metadata" in backend
          assert "Dir::State::lists=" in backend
          assert "APT::Sandbox::User=" in backend
          assert 'strcmp(argv[1], "refresh")' not in helper
          assert 'run_apt({"update"})' in helper
          assert 'strcmp(argv[1], "apply-plan")' in helper
          assert '"--no-remove"' in helper
          assert '"--no-install-recommends"' in helper
          assert "validate_apt_simulation" in helper
          assert 'simulation_arguments.insert(simulation_arguments.begin(), "-s")' in helper
          assert "introduced an unapproved change" in guard
          assert "parse_remove_line" in guard
          assert '"remove:"' in guard
          assert "approved.remove != actual.remove" in guard
          assert "plan_removal(" in transaction
          assert "Refusing to remove Essential package" in transaction
          assert "Authentication is required to install, update or remove system software." in policy

          # The update inventory must follow the host's Debian candidate policy,
          # not merely choose the numerically newest version from every source.
          assert "/etc/apt/preferences.d" in preferences
          assert "release_origin" in preferences
          assert "release_archive" in preferences
          assert "DebianPolicyStack" in package_policy
          assert "providers_" in package_policy
          assert "policy.add(phased_updates)" in reconcile
          assert "policy.add(host_preferences)" in reconcile
          assert "phased-update-percentage" in Path("src/engine/debian_package_index.cpp").read_text()
          assert "std::minstd_rand" in phased
          assert "APT::Get::Always-Include-Phased-Updates".lower() in phased.lower()
          assert "APT::Get::Never-Include-Phased-Updates".lower() in phased.lower()
          assert "preferences.priority_for" not in reconcile
          assert "candidate.pin_priority" in candidate
          assert "pin_priority INTEGER NOT NULL" in state_store
          assert "policy_provider TEXT NOT NULL" in state_store
          assert "release_origin TEXT NOT NULL" in state_store
          assert '"policy-provider"' in engine_service
          assert '"selection-reason"' in engine_service
          assert '"Policy: " + policy_name' in app
          assert '"Recommended update"' in app

          # The footer must report the exact pinned Common header version,
          # never a stale hand-written version string.
          assert "INFILTRATR_COMMON_VERSION" in app
          assert '"Common 1.19.10"' not in app

          # Repositories must be an operational settings surface. Mutable APT
          # and Flatpak sources expose a real Enabled/Disabled action, while
          # system APT edits stay behind the constrained source helper.
          assert "source_toggle_clicked" in app
          assert "button.source-state-toggle.state-installed label" in theme
          assert "button.source-state-toggle.state-available label" in theme
          assert '"set-apt-source-enabled"' in source_helper
          assert "safe_existing_apt_source" in source_helper
          assert "/etc/apt/sources.list.d" in source_helper
          assert "set_apt_list_entry_enabled" in source_mutation
          assert "set_apt_deb822_entry_enabled" in source_mutation

          # A package upgrade can leave the previous per-user D-Bus engine
          # process alive even when the interface API version is unchanged.
          # Clients must therefore match both API and exact package version and
          # recycle a stale same-user engine before any engine operation.
          assert "constexpr guint kApiVersion = 2U;" in engine_service
          assert '"engine-version"' in engine_service
          assert '<method name="Quit"/>' in engine_interface
          assert "kRequiredApiVersion = 2U" in engine_client
          assert "kRequiredEngineVersion" in engine_client
          assert "ensure_engine_identity(" in engine_client
          assert "GetConnectionUnixUser" in engine_client
          assert "GetConnectionUnixProcessID" in engine_client

          # Discover must remain an operational planned-install surface, not a
          # catalogue that permanently advertises an unavailable Install path.
          assert "Installation remains disabled until the transaction planner" not in app
          assert "discover_install_clicked" in app
          assert '"apply-plan"' in app
          assert "state->pending_update_plan = plan" in app
          assert "request.action == TransactionAction::remove" in backend

          # Privileged mutations must authenticate rather than being denied
          # merely because the desktop is remote/inactive. Never cache this
          # authorization because helper arguments are transaction-specific.
          for policy_path in (update_policy_path, source_policy_path):
              defaults = ET.parse(policy_path).getroot().find(".//defaults")
              assert defaults is not None
              assert defaults.findtext("allow_any") == "auth_admin"
              assert defaults.findtext("allow_inactive") == "auth_admin"
              assert defaults.findtext("allow_active") == "auth_admin"
          PY

      - name: Engine service smoke test
        shell: bash
        run: |
          set -uo pipefail
          set +e
          timeout 3s dbus-run-session -- ./build/infiltrator-software-engine >engine.out 2>engine.err
          status=$?
          set -e
          cat engine.out || true
          cat engine.err || true
          if [[ "$status" -ne 124 ]]; then
            echo "::error::Engine service exited unexpectedly during D-Bus smoke test (status $status)."
            exit "$status"
          fi

      - name: Launch smoke test
        shell: bash
        run: |
          set -uo pipefail
          set +e
          timeout 20s xvfb-run -a ./build/infiltrator-software >launch.out 2>launch.err
          status=$?
          set -e
          echo "launch exit status: $status"
          cat launch.out || true
          cat launch.err || true
          if [[ "$status" -eq 124 ]]; then
            echo "Software remained alive for the 20 second smoke window."
            exit 0
          fi
          echo "::error::Software exited unexpectedly during launch smoke test (status $status)."
          echo "Collecting debugger backtrace..."
          timeout 20s xvfb-run -a gdb --batch             -ex "set pagination off"             -ex run             -ex "thread apply all bt full"             --args ./build/infiltrator-software || true
          exit "$status"

      - name: Validate AppStream metadata
        run: appstreamcli validate debian/infiltrator-software.metainfo.xml

      - name: Verify Debian package
        shell: bash
        run: |
          set -euo pipefail
          chmod +x debian/rules
          dpkg-buildpackage -us -uc -b
          package="../infiltrator-software_$(tr -d '[:space:]' < VERSION)_amd64.deb"
          test -f "$package"
          test "$(dpkg-deb -f "$package" Package)" = "infiltrator-software"
          dpkg-deb -f "$package" Depends | grep -Eq '(^|, )pkexec([, ]|$)'
          dpkg-deb -f "$package" Depends | grep -Eq '(^|, )gpgv([, ]|$)'
          dpkg-deb -f "$package" Depends | grep -Eq 'libxapp'
          dpkg-deb -f "$package" Recommends | grep -Eq '(^|, )flatpak([, ]|$)'
          dpkg-deb --fsys-tarfile "$package" | tar -tf - > package-files.txt
          grep -qx './usr/bin/infiltrator-software' package-files.txt
          grep -qx './usr/bin/infiltrator-software-tray' package-files.txt
          grep -qx './usr/libexec/infiltrator-software-helper' package-files.txt
          grep -qx './usr/libexec/infiltrator-software-update-helper' package-files.txt
          grep -qx './usr/libexec/infiltrator-software-engine' package-files.txt
          grep -qx './usr/share/dbus-1/services/net.ssmith.infiltrator.software.Engine.service' package-files.txt
          grep -qx './usr/share/dbus-1/interfaces/net.ssmith.infiltrator.software.Engine.xml' package-files.txt
          grep -qx './usr/share/polkit-1/actions/net.ssmith.infiltrator.software.policy' package-files.txt
          grep -qx './usr/share/polkit-1/actions/net.ssmith.infiltrator.software.updates.policy' package-files.txt
          grep -qx './etc/xdg/autostart/infiltrator-software-tray.desktop' package-files.txt
          grep -qx './usr/share/icons/hicolor/scalable/status/infiltrator-software-up-to-date-symbolic.svg' package-files.txt
          grep -qx './usr/share/icons/hicolor/scalable/status/infiltrator-software-updates-available-symbolic.svg' package-files.txt
          grep -qx './usr/share/icons/hicolor/scalable/status/infiltrator-software-checking-symbolic.svg' package-files.txt
          grep -qx './usr/share/icons/hicolor/scalable/status/infiltrator-software-installing-symbolic.svg' package-files.txt
          grep -qx './usr/share/icons/hicolor/scalable/status/infiltrator-software-error-symbolic.svg' package-files.txt
          grep -qx './usr/share/applications/net.ssmith.infiltrator.software.desktop' package-files.txt
          grep -qx 'StartupWMClass=net.ssmith.infiltrator.software' debian/net.ssmith.infiltrator.software.desktop
          grep -qx 'Icon=net.ssmith.infiltrator.software' debian/net.ssmith.infiltrator.software.desktop
          grep -Fq 'gtk_window_set_icon_name(GTK_WINDOW(window), "net.ssmith.infiltrator.software")' src/app/main.cpp
          grep -Fq 'gtk_about_dialog_set_logo_icon_name(' src/app/main.cpp
          grep -qx './usr/share/icons/hicolor/scalable/apps/net.ssmith.infiltrator.software.svg' package-files.txt
          grep -qx './usr/share/icons/hicolor/scalable/apps/infiltrator-software.svg' package-files.txt
          grep -qx './usr/share/app-install/icons/infiltrator-software.svg' package-files.txt
          grep -qx './usr/share/metainfo/infiltrator-software.metainfo.xml' package-files.txt
          grep -qx './usr/share/fonts/truetype/infiltrator-software/mb_corpo_a_cond_regular.ttf' package-files.txt
          grep -qx './usr/share/fonts/truetype/infiltrator-software/mb_corpo_s_bold.ttf' package-files.txt
          grep -qx './usr/share/fonts/truetype/infiltrator-software/mb_corpo_s_regular.ttf' package-files.txt
          if grep -Eq '^\./usr/include/infiltratr/|^\./usr/lib/.*/libinfiltratr|/cmake/InfiltratrCommon/' package-files.txt; then
            echo "::error::Software package leaked Common development/runtime payloads."
            cat package-files.txt
            exit 1
          fi
