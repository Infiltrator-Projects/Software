infiltrator-software (0.3.39) unstable; urgency=high

  * Replace the procedural GTK/Cairo Discover hero with committed raster
    artwork taken from the approved Software visual target.
  * Render the authored hero through GtkPicture with cover scaling instead of
    reconstructing the mountains, planet, checker sphere, computer and colour
    ribbons from code.
  * Remove the now-obsolete procedural hero drawing and split-title CSS.
  * Install the raster artwork as a first-class package asset under
    /usr/share/infiltrator-software/artwork.
  * Keep a source-tree fallback path so development and launch-smoke builds
    render the same artwork before installation.
  * Strengthen CI and release-package verification so the raster asset cannot
    silently disappear or regress back to procedural drawing.
  * Replace the initially corrupted binary upload with the real optimized JPEG
    hero and verify its JPEG signature and packaged presence.
  * Correct the UI vision to make the raster-first art-direction rule explicit.

 -- Shannon Smith <infiltratr@yandex.com>  Fri, 25 Sep 2026 20:02:00 +1000

infiltrator-software (0.3.38) unstable; urgency=high

  * Continue visual convergence toward the approved Software reference.
  * Replace the flat Discover welcome header with native procedural artwork
    rendered directly in GTK/Cairo rather than baking the interface into an
    image asset.
  * Add the target's blue-violet sky, warm horizon, layered mountains, planet,
    retro-computer silhouette and cyan/violet/orange Infiltrator ribbon motif.
  * Keep Welcome copy as live GTK text for scaling and accessibility while
    separating the product name with a stronger violet accent.
  * Preserve the existing functional dashboard, explicit window controls and
    nested scrolling behaviour introduced in 0.3.37.
  * Document this seventh visual-convergence layer in the UI vision.
  * Update the visual CI contract for the live split-title artwork structure.

 -- Shannon Smith <infiltratr@yandex.com>  Fri, 25 Sep 2026 19:44:00 +1000

infiltrator-software (0.3.37) unstable; urgency=high

  * Fix the main-window sizing regression exposed by the growing Discover
    dashboard: keep the application explicitly resizable and remove the
    artificial widget minimum-size request from the top-level window.
  * Replace compositor-generated title buttons with explicit native minimise,
    maximise/restore and close controls wired directly to GtkWindow.
  * Keep the maximise icon synchronized when the window manager changes
    maximised state externally.
  * Add a real non-overlay vertical page scrollbar to Discover so the complete
    dashboard, search controls and catalogue remain reachable at normal window
    sizes.
  * Keep the virtualized application catalogue in its own bounded scroller so
    thousands of packages remain efficient while the surrounding page can
    scroll normally.
  * Give the page scrollbar a visible target-sized trough and thumb and extend
    CI contracts to prevent the window/scroll usability regression returning.

 -- Shannon Smith <infiltratr@yandex.com>  Fri, 25 Sep 2026 19:31:00 +1000

infiltrator-software (0.3.36) unstable; urgency=high

  * Continue the visual convergence toward the approved Software target.
  * Recompose the visible Discover dashboard around a real Featured
    Applications row using live catalogue artwork and package actions.
  * Add a compact Updates Available preview driven by the current update
    inventory, with direct navigation to the full Updates page.
  * Add target-like Repository Status and Recent Activity panels in a right
    rail, populated from the real source inventory and transaction history.
  * Add a colourful Keep your system healthy banner linked to Repair and fed
    by the live package-health diagnostics.
  * Load Updates, repository state, history and repair health asynchronously
    when Discover is first opened so dashboard panels become useful without
    blocking initial catalogue painting.
  * Strengthen the Welcome treatment, panel typography, featured cards,
    preview rows and target-like health-banner styling.
  * Extend CI and UI-vision contracts for the new dashboard composition.
  * Correct SourceKind qualification and the update-icon helper declaration
    exposed by hosted release compilation.

 -- Shannon Smith <infiltratr@yandex.com>  Fri, 25 Sep 2026 18:52:00 +1000

infiltrator-software (0.3.35) unstable; urgency=high

  * Bring the sidebar substantially closer to the approved visual target.
  * Replace the generic search/list/warning navigation glyphs with target-like
    Home, grid, refresh, monitor, repository, history and engineering icons.
  * Increase navigation icon scale and spacing while removing the boxed
    administrative look from unselected icons.
  * Replace the muted selected-row treatment with the vivid blue-violet
    full-row selection used by the target, with white selected icon/text.
  * Match the target navigation wording, remove the NAVIGATE heading and
    per-row chevrons, and narrow the sidebar to restore main-canvas space.
  * Add a real red Updates count badge driven by the current update inventory.
  * Replace the technical APT/Common footer with a graphical Settings /
    Preferences card and wire it to a native appearance-preferences window.
  * Extend visual CI contracts and UI-vision documentation for the sidebar
    convergence pass.
  * Declare the new native Settings callback before navigation construction,
    fixing the release compiler failure caught by hosted verification.
  * Preserve the exact pinned Common version in the bottom status bar after
    removing the technical sidebar footer.

 -- Shannon Smith <infiltratr@yandex.com>  Fri, 25 Sep 2026 18:18:00 +1000

infiltrator-software (0.3.34) unstable; urgency=high

  * Continue convergence toward the checked-in target by moving product
    identity and global search into the application titlebar.
  * Remove duplicate branding from the sidebar so navigation begins immediately
    below the chrome, matching the intended software-centre composition.
  * Replace Discover's generic page intro with a Welcome to Infiltrator
    Software hero and colourful visual status orbs.
  * Add direct graphical dashboard cards for Available Updates, System Health
    and Repositories; the cards navigate to their full pages and update their
    summary values when those real subsystems are loaded.
  * Synchronise the global search field with Discover search so typing in the
    application chrome immediately opens and filters the software catalogue.
  * Increase the default canvas for the richer dashboard composition and fix
    low-contrast Source and spotlight-button treatments visible in the live UI.
  * Extend CI contracts and UI-vision documentation for this fourth visual
    convergence pass.
  * Update the visual-regression contract to require titlebar-owned branding
    and explicitly reject the removed duplicate sidebar brand.

 -- Shannon Smith <infiltratr@yandex.com>  Fri, 25 Sep 2026 17:58:00 +1000

infiltrator-software (0.3.33) unstable; urgency=high

  * Continue convergence toward the checked-in Software UI target with a
    substantially more graphical Discover landing experience.
  * Add a dynamic Featured Software spotlight driven by the current filtered
    catalogue rather than static promotional data, with large application
    artwork, concise metadata chips and direct Explore/Install/Update/Remove
    actions.
  * Replace Discover's flat summary strip with a two-part showcase: the
    featured application surface plus a compact graphical At a Glance column
    for catalogue size, source and state.
  * Add an explicit Explore Software section treatment so the application grid
    reads as a browsable visual catalogue rather than the first item in an
    administrative form.
  * Add spotlight artwork wells, layered depth, richer typography and compact
    metadata chips while retaining native GTK behaviour and accessibility.
  * Record the third convergence layer in the UI vision and guard its core
    graphical primitives in CI.

 -- Shannon Smith <infiltratr@yandex.com>  Fri, 25 Sep 2026 17:40:00 +1000

infiltrator-software (0.3.32) unstable; urgency=high

  * Continue the GUI-first redesign toward the checked-in visual target.
  * Add graphical Discover category shortcuts so common areas can be reached
    directly without relying only on a conventional drop-down.
  * Rebuild update rows around artwork wells, compact version chips and source
    identity, reducing package-manager prose in the primary visual surface.
  * Group Updates into System, Applications, Libraries & runtimes and Other
    software sections with icon-led headers and counts.
  * Add a large live Repair health overview that changes between checking,
    healthy and attention-required states and keeps the detailed diagnostics
    below it.
  * Strengthen the visual hierarchy and styling for the new Discover shortcuts,
    grouped Updates and Repair health centre while retaining native GTK
    behaviour and the existing tested package engine.
  * Extend CI regression contracts to keep these second-pass visual primitives
    from silently disappearing.
  * Correct GTK/GObject and package-kind type conversions exposed by the
    release compiler before publishing the visual pass.
  * Update the source-contract checks to verify policy provenance remains
    available through the compact graphical update-row tooltip rather than
    requiring the removed administrative policy text.

 -- Shannon Smith <infiltratr@yandex.com>  Fri, 25 Sep 2026 17:15:00 +1000

infiltrator-software (0.3.31) unstable; urgency=high

  * Check in the approved Software visual north-star mock-up and UI vision as
    durable design targets for the GTK implementation.
  * Begin the GUI-first redesign without replacing the working package engine:
    add a branded graphical sidebar, navigation subtitles and icon tiles,
    richer page hero panels, colourful visual ribbons and icon-led summary
    cards.
  * Make Discover substantially more visual with larger artwork wells, richer
    cards, provenance chips and direct Install/Update/Remove actions alongside
    Details.
  * Rework Updates into a clearer visual action bar, strengthen History as an
    activity-card surface and give Repair a dedicated graphical recovery-action
    presentation.
  * Increase the default application canvas and add layered gradients, depth,
    richer semantic colour and stronger card hierarchy while retaining the
    Common typography/theme contract.
  * Update the UI design contract and CI so the checked-in mock-up and first
    redesign primitives cannot silently disappear.

 -- Shannon Smith <infiltratr@yandex.com>  Fri, 25 Sep 2026 16:50:00 +1000

infiltrator-software (0.3.30) unstable; urgency=high

  * Make Updates refresh substantially faster by reusing verified Packages
    indexes when the current signed Release metadata is unchanged.
  * Reconcile installed dpkg state locally after package transactions instead
    of immediately repeating a full network repository refresh.
  * Treat stale update plans whose exact final versions are already installed
    as successful no-ops, clearing completed updates instead of recording a
    false failed transaction.
  * Implement the Repair page with live engine/source/dpkg diagnostics,
    interrupted-package detection, verified repository-state rebuild and a
    constrained authenticated dpkg --configure -a recovery action.
  * Give History a permanently visible vertical scrollbar and correct the
    low-contrast Exact versions detail treatment.
  * Keep assert-based regression checks active in Release builds and extend CI
    contracts for the Updates, Repair, repository-cache and History fixes;
    remove duplicate dependency diagnostics exposed by the restored assertions.
  * Update performance, recovery and roadmap documentation to match the
    implemented behaviour.
  * Isolate hosted release verification from the development self-hosted
    runner queue so an unavailable local runner cannot strand publication.

 -- Shannon Smith <infiltratr@yandex.com>  Fri, 25 Sep 2026 15:45:00 +1000

infiltrator-software (0.3.29) unstable; urgency=high

  * Complete the forensic functionality, documentation and code-comment audit.
  * Honour effective Debian pin priority in dependency and transaction-root
    selection, and reject incompatible final-state dependency requirements.
  * Make package-state schema migrations atomic and current-generation reads
    snapshot-consistent.
  * Make APT source mutation safe for in-place input/output use and keep
    deb822 LF/CRLF stanza identity consistent between inventory and mutation.
  * Bind long-running GUI tasks to the owning window lifetime and publish the
    post-install engine generation before Installed and Discover reload.
  * Correct the Common atomic-write success convention used by catalogue
    snapshots and align documentation with implemented progress/recovery scope.

 -- Shannon Smith <infiltratr@yandex.com>  Fri, 25 Sep 2026 14:00:00 +1000

infiltrator-software (0.3.28) unstable; urgency=medium

  * Align the sidebar footer with the suite-wide solid divider treatment.
  * Preserve navigation, package behaviour, dependencies and Common APIs
    unchanged.

 -- Shannon Smith <infiltratr@yandex.com>  Thu, 24 Sep 2026 21:31:00 +1000

infiltrator-software (0.3.27) unstable; urgency=medium

  * Align page hero titles with the 28 px publisher-wide desktop heading scale.
  * Keep navigation, cards, package behaviour, dependencies and Common APIs
    unchanged.

 -- Shannon Smith <infiltratr@yandex.com>  Thu, 24 Sep 2026 19:12:00 +1000

infiltrator-software (0.3.26) unstable; urgency=high

  * Restore readable repository navigation contrast on dark/system themes by
    using the proven information accent instead of the low-contrast operation
    palette entry for the repository glyph.
  * Keep the Repositories page hero icon, title and border on the same readable
    semantic accent, and force the sidebar symbolic icon to full opacity.

 -- Shannon Smith <infiltratr@yandex.com>  Thu, 24 Sep 2026 17:20:00 +1000

infiltrator-software (0.3.25) unstable; urgency=high

  * Keep privileged update activity visibly on the Updates page from
    authorization through metadata refresh, exact-plan revalidation, package
    execution and final installed-state verification.
  * Show an animated indeterminate progress bar with elapsed-time status so a
    long transaction cannot look idle while the helper is still working.
  * Keep the live durable History page and exact successful/failed transaction
    recording introduced in 0.3.23; remove the old milestone-only behaviour
    exposed by older installed builds.

 -- Shannon Smith <infiltratr@yandex.com>  Thu, 24 Sep 2026 16:05:00 +1000

infiltrator-software (0.3.24) unstable; urgency=high

  * Fix the dark update-row icon on dark/system themes.
  * Stop relying on Mint's software-update-available-symbolic asset for
    application update rows because some icon-theme variants are pre-coloured
    and do not inherit the Software palette.
  * Use a reliably symbolic refresh glyph and explicitly apply the operation
    colour and full opacity to package icons.

 -- Shannon Smith <infiltratr@yandex.com>  Thu, 24 Sep 2026 15:46:00 +1000

infiltrator-software (0.3.23) unstable; urgency=high

  * Replace the History milestone placeholder with a live durable transaction
    view.
  * Persist approved install, update and removal outcomes in a per-user SQLite
    history database with exact before/after versions, package actions, source
    provenance, requested/system-critical flags and transaction identifiers.
  * Record both successful and failed approved transactions from Updates and
    Discover, and refresh History immediately when it is already open.
  * Keep a visible indeterminate progress indicator and elapsed-time status
    throughout privileged update execution so a long-running install no longer
    appears to have stopped.
  * Preserve a distinct final-state verification phase after package mutation,
    then report the verified completion state or any remaining preferred
    updates.
  * Add automated transaction-history storage coverage and CI guards against
    restoring the History placeholder or removing install-activity feedback.

 -- Shannon Smith <infiltratr@yandex.com>  Thu, 24 Sep 2026 15:32:00 +1000

infiltrator-software (0.3.22) unstable; urgency=high

  * Replace the System milestone placeholder with a live system-components page.
  * Classify installed kernel, driver and core operating-system packages through
    the shared package model so the engine and GUI use one definition.
  * Show installed versions, preferred update targets, component type,
    architecture and system-critical state for system packages.
  * Add component/update/critical counters, an asynchronous repository refresh
    control and a Review system updates action that hands off to the unified
    Updates workflow rather than creating a second updater.
  * Keep System page loading lazy and off the GTK thread.

 -- Shannon Smith <infiltratr@yandex.com>  Thu, 24 Sep 2026 14:45:00 +1000

infiltrator-software (0.3.21) unstable; urgency=high

  * Make the Updates page self-explanatory: every listed package is now
    explicitly presented as the preferred/recommended candidate selected by
    the active policy stack.
  * Preserve repository origin/site, policy provider, policy reason and
    resolved candidate priority in package-state schema version 4.
  * Carry that provenance through the shared D-Bus engine into the GUI instead
    of exposing only the raw APT source-file identity.
  * Show human-readable Source and Policy lines for every update, including
    Host preferred, Infiltrator preferred and repository-default candidates.
  * Keep the complete policy/selection rationale as a tooltip and label
    system-critical packages as recommended rather than merely available.
  * Match APT phased-update eligibility natively using source package/version,
    the host machine ID and APT's deterministic minstd_rand bucket algorithm,
    so staged Ubuntu updates do not appear as recommended before this machine
    is eligible.
  * Honour the standard Always/Never Include Phased Updates compatibility
    settings while keeping normal update discovery free of APT subprocesses.

 -- Shannon Smith <infiltratr@yandex.com>  Thu, 24 Sep 2026 14:12:00 +1000

infiltrator-software (0.3.20) unstable; urgency=medium

  * Separate Debian package mechanics from distribution policy behind a
    provider interface and ordered policy stack.
  * Keep the current host APT preferences as a compatibility provider rather
    than making Mint/APT policy the permanent Infiltrator resolver model.
  * Define provider precedence so future Infiltrator OS policy can override
    only explicitly migrated packages while untouched packages retain safe
    host policy.
  * Preserve existing 0.3.19 candidate behaviour on current systems; this is an
    architectural decoupling, not a change to the active host policy.
  * Add policy-stack precedence/fallback tests and CI guards, and document the
    staged Mint-to-Infiltrator/Debian-derived migration path.

 -- Shannon Smith <infiltratr@yandex.com>  Thu, 24 Sep 2026 13:10:00 +1000

infiltrator-software (0.3.19) unstable; urgency=high

  * Fix false native update candidates on Linux Mint by applying the host APT
    preference/pinning policy instead of treating every configured repository
    as equal priority.
  * Parse repository Origin/Label/Version/Suite/Codename/component/site policy
    identity and honour /etc/apt/preferences plus preferences.d pin rules.
  * Honour Release NotAutomatic and ButAutomaticUpgrades default priorities and
    persist the resolved candidate priority in package-state schema version 3.
  * Add regression coverage for the Mint upstream-vs-Ubuntu base-files case so
    a numerically newer lower-priority Ubuntu version is not reported as an
    update over the preferred Mint package.
  * Replace the stale hard-coded Common 1.19.10 footer with the exact pinned
    INFILTRATR_COMMON_VERSION value (currently Common 1.19.24).

 -- Shannon Smith <infiltratr@yandex.com>  Thu, 24 Sep 2026 12:25:00 +1000

infiltrator-software (0.3.18) unstable; urgency=high

  * Fix unreadable Enabled/Disabled repository toggle labels in dark/system
    themes after the state badges became interactive GtkButtons.
  * Style both the repository-state button and its child label explicitly so
    GTK's generic button-label foreground rule cannot override the state colour.
  * Preserve distinct green Enabled and blue Disabled states, including hover,
    with full label opacity and pill-shaped borders in every palette.
  * Add CI guards for both repository toggle label selectors.

 -- Shannon Smith <infiltratr@yandex.com>  Thu, 24 Sep 2026 11:55:00 +1000

infiltrator-software (0.3.17) unstable; urgency=high

  * Fix in-session package upgrades leaving the previous native engine binary
    alive when the D-Bus API version itself did not change.
  * Report the exact Software package version from the engine GetStatus reply
    and require both package version and API version to match before every
    engine operation.
  * Recycle only the verified same-user stale engine, then let D-Bus activate
    the newly installed binary before retrying.
  * This ensures the 0.3.16 ASCII-armored Signed-By fix is actually used
    immediately after upgrading from 0.3.15 without requiring logout/reboot.

 -- Shannon Smith <infiltratr@yandex.com>  Thu, 24 Sep 2026 11:20:00 +1000

infiltrator-software (0.3.16) unstable; urgency=high

  * Fix native repository signature verification for APT Signed-By keyrings
    stored as ASCII-armored .asc files.
  * Dearmor configured .asc public keys into private temporary binary keyrings
    for gpgv without modifying the administrator's configured key files.
  * Validate OpenPGP armor and CRC24 before trusting the converted key data.
  * Add an end-to-end repository regression fixture matching Docker's
    recommended /etc/apt/keyrings/docker.asc configuration.

 -- Shannon Smith <infiltratr@yandex.com>  Thu, 24 Sep 2026 10:51:00 +1000

infiltrator-software (0.3.15) unstable; urgency=high

  * Make repository Enabled/Disabled state an operational graphical control
    for configured APT sources and Flatpak remotes.
  * Preserve exact APT line/stanza identities so a toggle changes only the
    selected source, including traditional .list and deb822 .sources files.
  * Apply system APT changes through the authenticated source helper with
    strict /etc/apt path validation, atomic replacement and owner/mode
    preservation.
  * Refresh Repositories, Discover and loaded Updates state after a successful
    source change so disabled repositories immediately leave reconciliation.
  * Add regression tests for list/deb822 enable-disable mutation and protect
    the source-management contract in CI.

 -- Shannon Smith <infiltratr@yandex.com>  Thu, 24 Sep 2026 10:00:00 +1000

infiltrator-software (0.3.14) unstable; urgency=high

  * Version the package-engine D-Bus contract at API v2 for RefreshState.
  * Detect stale pre-v2 per-user engine processes left alive across package
    upgrades and recycle only the verified same-user service before refresh.
  * Add a graceful Quit method for subsequent in-session engine upgrades.
  * Guard the lifecycle contract in CI and document the activation behaviour.

 -- Shannon Smith <infiltratr@yandex.com>  Wed, 23 Sep 2026 20:01:00 +1000

infiltrator-software (0.3.13) unstable; urgency=medium

  * Add native Discover package removal with installed reverse-dependency,
    virtual-provides and Essential-package safety checks.
  * Persist installed dependency, Pre-Depends, Provides, Priority, Multi-Arch
    and Essential metadata in package-state schema version 2.
  * Extend exact privileged-plan revalidation to explicitly approved removals
    while continuing to reject any unapproved APT mutation.
  * Add per-package and arbitrary selected-update workflows with Select all and
    Clear controls, preserving complete native preflight before authorization.
  * Correct roadmap, package-engine, state and authorization documentation for
    the implemented workflows.

 -- Shannon Smith <infiltratr@yandex.com>  Wed, 23 Sep 2026 19:38:00 +1000

infiltrator-software (0.3.12) unstable; urgency=medium

  * Replace the repository engine's private exact-write loop with Common
    1.19.24's equivalent EINTR-safe infiltratr_write_full contract.
  * Preserve the exact approved APT transaction guard and document its
    architecture and one-to-one mutation-set invariants.
  * Treat central Infiltrator-Repository publication as asynchronous unless an
    immediate cross-repository dispatch is actually accepted.
  * Keep the current Common 1.19.24 pin and existing 0.3.11 transaction safety
    behaviour unchanged.

 -- Shannon Smith <infiltratr@yandex.com>  Wed, 23 Sep 2026 14:10:00 +1000

infiltrator-software (0.3.11) unstable; urgency=high

  * Re-pin Software to Common 1.19.24 at 748e089ae175329471d4cf375522c44081371bd5.
  * Re-simulate the exact approved APT transaction after the privileged
    repository-metadata refresh and before package mutation.
  * Abort if refreshed metadata adds a dependency, drops an approved change,
    changes architecture, requests removal, or otherwise differs from the
    transaction the user reviewed.
  * Add focused regression tests for exact transaction matching, stale plans,
    unexpected dependencies, removals, architecture drift and duplicates.
  * Extend CI to enforce the post-refresh validation boundary.

 -- Shannon Smith <infiltratr@yandex.com>  Tue, 22 Sep 2026 18:45:00 +1000

infiltrator-software (0.3.10) unstable; urgency=high

  * Enable Discover installation instead of permanently presenting the
    transaction-planner placeholder.
  * Resolve native install/update dependency graphs asynchronously and show the
    complete package change set before requesting administrator authorization.
  * Fall back to the transitional APT planner when a shared native generation is
    not yet available, while still prohibiting package removal.
  * Execute every approved install/upgrade plan item at its exact reviewed
    version and disable implicit Recommends/Suggests in the privileged bridge.
  * Make Updates execute the exact resolved plan rather than reconstructing a
    second root-only transaction after confirmation.
  * Refresh Discover, Installed and Updates state after a successful install.
  * Add CI regression checks preventing Discover installation from being
    silently disabled again.

 -- Shannon Smith <infiltratr@yandex.com>  Tue, 22 Sep 2026 12:45:00 +1000

infiltrator-software (0.3.9) unstable; urgency=high

  * Fix privileged update installation from remote or inactive graphical
    sessions: PolicyKit now requests administrator authentication instead of
    denying those subject classes before an authentication dialog can appear.
  * Apply the same authorization semantics to repository-source mutations so
    Add Source does not fail for the same reason.
  * Require fresh administrator authentication for every privileged mutation;
    do not retain authorization across transaction-specific helper arguments.
  * Add CI regression checks for active, inactive and remote PolicyKit defaults.

 -- Shannon Smith <infiltratr@yandex.com>  Tue, 22 Sep 2026 12:12:00 +1000

infiltrator-software (0.3.8) unstable; urgency=medium

  * Keep Updates fresh after the first automatic refresh instead of freezing
    repository metadata for the rest of the Software process lifetime.
  * Refresh repository metadata automatically when the user re-enters Updates
    after one minute and while the Updates page remains open and stale.
  * Keep all refresh work asynchronous and unprivileged; package mutation still
    crosses the existing authorization boundary only after a transaction plan.
  * Add regression coverage proving Debian-managed hardware-native builds such
    as Calendar +nativepgo packages remain visible in installed state and
    compare correctly against later generic repository releases.
  * Document that Calendar's native .run installer already installs a real
    Debian package; no parallel unmanaged-install receipt is required.

 -- Shannon Smith <infiltratr@yandex.com>  Mon, 21 Sep 2026 21:15:00 +1000

infiltrator-software (0.3.7) unstable; urgency=medium

  * Replace Discover's eager GtkFlowBox with a virtualized GtkGridView so only visible application cards are constructed.
  * Cache folded Discover search text instead of rebuilding every application's search corpus on each keystroke.
  * Bulk-update the Installed package model instead of emitting thousands of individual list changes.
  * Fail fast when the optional shared package engine is unavailable instead of allowing read-only inventory calls to wait up to ten seconds.
  * Move repository inventory reads completely off the GTK main thread.
  * Remove the major UI-thread and widget-construction stalls that made navigation, Discover, Installed and Repositories feel globally slow.

 -- Shannon Smith <infiltratr@yandex.com>  Mon, 21 Sep 2026 20:40:00 +1000

infiltrator-software (0.3.6) unstable; urgency=medium

  * Treat cached APT compatibility metadata as a fast first paint, not a permanent source of truth.
  * Automatically perform one unprivileged repository refresh after cached update state is shown.
  * Replace the cached update list with current repository candidates without blocking the Updates page.
  * Avoid falsely reporting "Your system is up to date" indefinitely after a newer Software release has been published.

 -- Shannon Smith <infiltratr@yandex.com>  Mon, 21 Sep 2026 20:14:00 +1000

infiltrator-software (0.3.5) unstable; urgency=medium

  * Render the saved Discover catalogue immediately instead of waiting for package-engine installed-state reconciliation.
  * Move Discover installed-state reconciliation into a second background task after the first usable page paint.
  * Merge asynchronous icon hydration into the current catalogue instead of replacing records and racing installed-state updates.
  * Eliminate the multi-second blank Discover window seen on close/reopen with the 0.3.4 shared-engine migration.
  * Include the suite-standard About dialog/build-profile identity update.

 -- Shannon Smith <infiltratr@yandex.com>  Mon, 21 Sep 2026 19:42:00 +1000

infiltrator-software (0.3.4) unstable; urgency=medium

  * Add one typed D-Bus client for the shared native package engine.
  * Make Installed and ordinary Updates inventory consume shared engine generations first.
  * Move Installed inventory work off the GTK main thread and retain a safe direct Debian-state fallback while native state publication is completed.
  * Use native engine transaction planning whenever the displayed update inventory came from the shared engine.
  * Make the XApp panel indicator consume engine update state and react to StateChanged and HealthChanged signals instead of independently resolving the same generation.
  * Preserve the unprivileged APT compatibility refresh path until native source reconciliation owns repository refresh end to end.

 -- Shannon Smith <infiltratr@yandex.com>  Mon, 21 Sep 2026 18:55:00 +1000

infiltrator-software (0.3.3) unstable; urgency=medium

  * Make sidebar navigation switch GtkStack pages atomically instead of crossfading old and new page contents.
  * Defer lazy page refresh startup until the next GTK idle cycle so the destination page is painted before catalogue work begins.
  * Prevent History and Discover widgets from appearing simultaneously during a slow Discover load.

 -- Shannon Smith <infiltratr@yandex.com>  Mon, 21 Sep 2026 18:05:00 +1000

infiltrator-software (0.3.2) unstable; urgency=medium

  * Stop requesting administrator credentials when the user refreshes package metadata.
  * Refresh transitional APT metadata into an isolated per-user cache and use that cache for update inventory and preflight planning.
  * Keep authorization at the mutation boundary only: the privileged installer refreshes root-owned system metadata after approval, then applies the exact requested versions.
  * Update the Polkit message and add regression checks preventing a privileged refresh path from returning.

 -- Shannon Smith <infiltratr@yandex.com>  Sun, 20 Sep 2026 19:05:00 +1000

infiltrator-software (0.3.1) unstable; urgency=medium

  * Bind the GTK application window explicitly to the canonical
    infiltrator-software icon.
  * Use the same project-owned icon in the About dialog.
  * Publish the GTK application WM class so Cinnamon taskbar matching does not
    fall back to a generic application icon.
  * Keep the 0.3.0 update engine and panel updater behaviour unchanged.

 -- Shannon Smith <infiltratr@yandex.com>  Sun, 20 Sep 2026 16:40:00 +1000

infiltrator-software (0.3.0) unstable; urgency=medium

  * Replace the Updates placeholder with live asynchronous APT update inventory.
  * Resolve and present the complete APT transaction before update execution.
  * Add a narrow Polkit-authorized update helper restricted to exact-version
    upgrades of packages that are already installed, with removals prohibited.
  * Add privileged package-list refresh and integrated Install all updates flow.
  * Add a Mint-native XApp panel update indicator with distinct up-to-date,
    orange update-available, checking, installing and red error states.
  * Autostart the panel indicator, keep it single-instance and start it when
    Software opens so existing installations do not require a new login.
  * Open Software directly on Updates from the panel indicator.
  * Package and verify the updater helper, Polkit policy, autostart entry,
    XApp runtime dependency and complete status-icon set in CI and releases.

 -- Shannon Smith <infiltratr@yandex.com>  Sun, 20 Sep 2026 16:10:00 +1000

infiltrator-software (0.2.5) unstable; urgency=medium

  * Fix AppStream component-box ownership after the merged system catalogue load.
  * Keep the AsPool as the sole owner of its returned component collection.
  * Retain the unified Discover view across Infiltrator, native AppStream/APT and configured Flatpak remotes.
  * Retain the integrated Repositories source inventory and Add Source workflow.
  * Verify the complete application under the permanent graphical launch smoke gate.

 -- Shannon Smith <infiltratr@yandex.com>  Sun, 20 Sep 2026 15:44:00 +1000

infiltrator-software (0.2.4) unstable; urgency=medium

  * Standardise the application icon on the canonical #00ADEF Infiltrator blue.
  * Keep host AppStream ingestion focused on native distribution applications.
  * Isolate Flatpak catalogue enumeration behind the flatpak CLI instead of loading Flatpak metadata through the in-process AppStream pool.
  * Preserve one merged Discover view across Infiltrator, APT/AppStream and configured Flatpak remotes.
  * Keep the integrated Repositories/Add Source workflow from 0.2.3 unchanged.
  * Retain the graphical launch smoke gate after fixing the AppStream/Flatpak catalogue crash path.

 -- Shannon Smith <infiltratr@yandex.com>  Sun, 20 Sep 2026 15:12:00 +1000

infiltrator-software (0.2.3) unstable; urgency=medium

  * Merge host AppStream applications and configured Flatpak metadata into Discover.
  * Preserve verified Infiltrator records while suppressing duplicate native package identities.
  * Reconcile native APT and Flatpak installed state in the unified catalogue.
  * Replace the Repositories placeholder with live Infiltrator, APT and Flatpak source inventory.
  * Add an integrated Add Source workflow for HTTPS APT repositories and user Flatpak remotes.
  * Add a constrained Polkit-authorized helper for atomic Deb822 APT source creation.
  * Harden privileged source parameters against newline, whitespace and path injection.
  * Add source parser regression coverage and retain the graphical startup smoke gate.

 -- Shannon Smith <infiltratr@yandex.com>  Sun, 20 Sep 2026 12:10:00 +1000

infiltrator-software (0.2.2) unstable; urgency=medium

  * Fix a Discover startup SIGSEGV caused by replacing a dropped GtkDropDown model.
  * Keep the Discover category GtkStringList alive for the lifetime of the window and mutate it in place.
  * Delay initial Discover navigation selection until all GtkStack pages exist.
  * Add a permanent 20-second Xvfb launch smoke test so startup/runtime crashes fail CI.
  * Capture an automatic debugger backtrace if the launch smoke test fails.

 -- Shannon Smith <infiltratr@yandex.com>  Sun, 20 Sep 2026 11:38:00 +1000

infiltrator-software (0.2.1) unstable; urgency=medium

  * Render Discover catalogue metadata immediately instead of waiting for icon downloads.
  * Move repository icon verification into a second generation-checked background task.
  * Keep application cards usable with category fallback icons while verified icons load.
  * Prevent slow or failed icon downloads from leaving Discover stuck at 0 applications / Loading.

 -- Shannon Smith <infiltratr@yandex.com>  Sun, 20 Sep 2026 10:40:00 +1000

infiltrator-software (0.2.0) unstable; urgency=medium

  * Implement Discover using the published Infiltrator application catalogue.
  * Add asynchronous HTTPS catalogue refresh with atomic offline cache fallback.
  * Verify repository-provided application icons by SHA-256 before caching.
  * Add live search, category filtering, application cards and provenance details.
  * Merge catalogue identities with local dpkg inventory to show Installed/Available state.
  * Keep installation disabled until complete transaction planning and authorization are implemented.

 -- Shannon Smith <infiltratr@yandex.com>  Sun, 20 Sep 2026 02:28:00 +1000

infiltrator-software (0.1.2) unstable; urgency=medium

  * Move Software to the exact Common 1.19.10 contract used by InfiltratorFS.
  * Use the complete MBLINK-derived semantic palette in System, Day and Night modes.
  * Match the proven InfiltratorFS typography scale and content spacing.
  * Correct title-bar button foreground, icon and hover contrast.
  * Add semantic colour treatment for navigation, status cards, pages and package rows.

 -- Shannon Smith <infiltratr@yandex.com>  Sun, 20 Sep 2026 02:04:00 +1000

infiltrator-software (0.1.1) unstable; urgency=medium

  * Align the native application shell with the InfiltratorFS design language.
  * Add centered title/subtitle header bar with info, theme and refresh controls.
  * Move System/Day/Night theme cycling into the title bar while retaining Common 1.19.8.
  * Add Infiltrator-style sidebar, cards, package summary panels and status bar.
  * Preserve the existing read-only APT backend and package-management boundaries.

 -- Shannon Smith <infiltratr@yandex.com>  Sun, 20 Sep 2026 01:52:00 +1000

infiltrator-software (0.1.0) unstable; urgency=medium

  * Establish the greenfield Infiltrator Software Manager architecture.
  * Add Discover, Installed, Updates, System, Repositories, History and Repair.
  * Add a read-only APT/.deb backend and backend-neutral transaction model.
  * Add persistent Common 1.19.8 Follow OS, Day and Night appearance modes.
  * Package the canonical MB Corpo font set, desktop integration and AppStream metadata.
  * Add verified Debian packaging and immutable GitHub release automation.

 -- Shannon Smith <infiltratr@yandex.com>  Sun, 20 Sep 2026 01:20:00 +1000
